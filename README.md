# global-workflow-patch
This repository contains patch files to build and run SFS via externalized containers and the global-workflow.
The patch file is currently configured to allow for building on the GMU Hopper and TACC Stampede3 systems
